import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardImage: {
    flex: 1,
    width: '100%',
    height: '100%',
    resizeMode: 'cover'
  }
})

export default styles;
